{
  "name": "@agent-harness/examples",
  "version": "0.1.0",
  "private": true,
  "description": "CLI examples for @agent-harness/core.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && node dist/basic.js",
    "chat": "tsc -p tsconfig.json && node dist/chat.js",
    "real-loop": "tsc -p tsconfig.json && node dist/real-loop.js",
    "demo:env": "tsc -p tsconfig.json && node dist/self-serve-env.js",
    "verify:harness": "tsc -p tsconfig.json && node dist/verify-harness.js",
    "verify:mcp": "tsc -p tsconfig.json && node dist/verify-mcp.js",
    "verify:context7": "tsc -p tsconfig.json && node dist/verify-context7.js",
    "use:context7": "tsc -p tsconfig.json && node dist/use-context7.js",
    "mcp:multi": "tsc -p tsconfig.json && node dist/multi-mcp.js",
    "medspa": "tsc -p tsconfig.json && node dist/medspa-agent.js",
    "shell": "tsc -p tsconfig.json && node dist/shell.js",
    "workflow": "tsc -p tsconfig.json && node dist/workflow-demo.js",
    "multi-agent": "tsc -p tsconfig.json && node dist/multi-agent.js",
    "os-sandbox": "tsc -p tsconfig.json && node dist/os-sandbox.js",
    "rag-e2e": "tsc -p tsconfig.json && node dist/rag-e2e.js",
    "verify": "tsc -p tsconfig.json && node dist/verify-harness.js && node dist/verify-mcp.js && node dist/verify-context7.js"
  },
  "license": "MIT",
  "dependencies": {
    "@agent-harness/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.4.5"
  }
}
