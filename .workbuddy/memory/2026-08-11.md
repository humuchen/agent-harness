# 2026-08-11

## Render 部署配置
- 目标：把 `src/ui/server.ts` 的 SSE 仪表盘作为 Render **Web Service** 部署。
- 关键修复：`server.ts` 原只读取 `UI_PORT`，Render 注入的是 `PORT`，会导致服务不可达。已改为 `PORT ?? UI_PORT ?? 4173`，并默认绑定 `0.0.0.0`（无需改）。
- 新增 `render.yaml`（Blueprint）：runtime node、plan free、build=`npm install && npm run build`、start=`node dist/src/ui/server.js`、healthCheckPath=`/api/state`。`dist/` 已被 gitignore，故 Render 在构建期编译。
- `package.json` 加 `engines.node: 22.x` 锁定版本。
- 已本地 `npm run build` 通过，并用 `PORT=8080` 冒烟测试：`/api/state` 与 `/` 均 200，MCP 自动连上 Context7。
- 已 commit 并 `git push origin dev`（565338e）。剩余步骤在 Render Dashboard 用 Blueprint 连仓库选 dev 分支即可；OPENROUTER_API_KEY 留空则走 Mock 模式，填了才走真实 LLM。
