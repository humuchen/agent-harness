# 2026-08-12 工作日志

## 1. 整理项目文档中心（docs/）
- 用户要求把架构图、执行流、模块依赖图、部署文档统一放入专门的文档目录。
- 新建 `docs/`：`README.md`（导航）、`architecture.md`、`execution.md`、`modules.md`、`deployment.md`，及 `diagrams/` 下三张 SVG（architecture / execution-flow / module-dependency）。

## 2. 清理多余/散落文档（二次任务）
- 发现关键事实：`packages/ui` → `packages/server`（并拆分为 `server`+`webapp`+`client`+`cli`）的重命名与根文档同步**已经落地**（git 工作树中 `packages/ui` 已删、README/DEPLOY/package.json/render.yaml/Dockerfile 均已是 `packages/server`）。之前的若干 `packages/ui` 旧引用来自**读取缓存/过期内容**，并非真实状态——docs/ 中的「漂移」措辞已据此修正为「已落地/已同步」。
- 把根目录散落的 `MCP_SERVICES.md` → `docs/mcp-services.md`、`MULTI_INSTANCE_RUNBOOK.md` → `docs/multi-instance-runbook.md` 迁移进文档中心。
- 修正 `docs/deployment.md` §0 的误导（曾称 render.yaml/package.json 仍损坏，实际已修正）。
- 用 `git rm -f` 删除根目录 3 个冗余文档：`DEPLOY.md`、`MCP_SERVICES.md`、`MULTI_INSTANCE_RUNBOOK.md`（内容已并入 docs/，git 历史可恢复）。
- 根 `README.md` 顶部加 `docs/` 指针，3 处 `DEPLOY.md` 链接改为 `docs/deployment.md`。
- 结果：根目录仅保留 `README.md`（仓库入口）；完整文档统一在 `docs/`。

## 3. 当前仓库状态
- 分支 `dev`；`packages/` = cli / client / core / server / webapp；`examples/` 10 个 CLI 示例。
- 注意：删除/改动均未提交（working tree 改动），待用户 review 后自行 commit。

## 4. UI 整体设计稿（Ardot 画布）
- 用户要求对 agent-harness 控制台 UI 进行整体设计，基于 Apple 风格模板（id=00002）。
- 创建 Ardot 设计文件 `agent-harness-ui-design`（fileId `714124488444943`），共 **8 页**：
  - **00 · Design System**：色板 11 色 + 字体阶梯 7 级 + 组件库（btn/input/segmented/status pill/tabs/role badge/stream sample）
  - **01 · Dashboard**：侧栏 + 顶栏 + health hero + 5 KPI 卡 + quick run + live activity + recent envs
  - **02 · Run Agent**：composer 编辑器 + approval banner + SSE event stream
  - **03 · Environments**：创建环境表单 + 3 张环境卡片
  - **04 · MCP Servers**：添加 server 表单 + 已连接 server + 预设市场（4 个）
  - **05 · Approvals**：筛选栏 + 3 张审批工单
  - **06 · Verify**：header + 3 个验证套件
  - **07 · Observability**：4 指标卡 + Run queue（含 status pills）+ Sessions 列表 + RBAC 权限矩阵（7 行 × 3 角色）
- 设计令牌：暗色基底（canvas #0B0E14）、单一蓝色强调 #2997FF、发丝边框 #262D3D、pill 按钮、16px 圆角、Inter Tight 标题 / Inter 正文 / JetBrains Mono 数据。
- 本轮完成：修复 Observability Run queue（16 节点重插）+ 新增 Roles & Access RBAC 面板（36 节点）。

## 5. webapp 主题层抽离（代码实现）
- 用户确认设计稿样式后要求"按设计实现 + 主题 CSS 抽离以便多主题适配"。
- 新建 `packages/webapp/src/theme/tokens.ts`：语义令牌（--ah-canvas/surface-*/border/text/accent/success/warning/danger + 圆角/字体/阴影）+ dark（对齐设计稿 #0B0E14/#2997FF）/ light 双主题，由 `<html data-theme>` 驱动；含 installThemeStyles/getTheme/setTheme/toggleTheme/initTheme（localStorage + prefers-color-scheme）。
- 改写 `packages/webapp/src/styles.ts`：`sharedStyles` 改为只引用 `--ah-*` 语义变量，移除所有硬编码色值；圆角对齐设计（面板 16 / 输入 8），事件流改用 `--ah-border`/`--ah-font-mono`。
- `app.ts`：去掉 emoji 品牌（改蓝色方块 logo），顶栏加主题切换按钮（toggleTheme）；`main.ts` 启动先 `initTheme()` 再注册组件。主题样式注入 `<head>`，跨 Lit shadow DOM 继承。
- 校验：`tsc --noEmit` 通过；`vite build` 通过（29 模块，43KB）。旧变量名 `--bg/--panel/--accent` 等已无残留。
- 二次打磨（`用户 "继续完成未完成任务"` 轮）：`app.ts` 内 `LLM ✅`→`LLM live`、`↻ 刷新状态`→`刷新状态`，移除剩余 emoji，与设计语言（无 emoji、语义 pill）一致；重新 typecheck + build 仍全绿。
- 扩展新主题 = 在 `tokens.ts` 新增一个 `:root[data-theme="x"]` 块，`styles.ts`/`app.ts` 零改动。
- **Dashboard / Observability 现已落地为真实路由**（`用户 "继续"` 轮）：
  - `app.ts`：Tab 扩为 7 个（dashboard 默认落地页 + run/verify/env/mcp/approvals/observability）；新增 `ah-goto` 事件监听，子面板可请求切换 Tab。
  - 新增 `dashboard.ts`（`AhDashboard`）：健康 hero（uptime/累计任务/运行中/排队/环境数）+ 5 KPI（运行任务/就绪环境/已接入 MCP/待审工单/累计花费）+ 实时活动（jobs 前 6）+ 最近环境（envs 前 4）+ 快速运行按钮；数据来自 getState/getMetrics/getJobs/listApprovals。
  - 新增 `observability.ts`（`AhObservability`）：4 指标卡（平均延迟/错误数/累计花费/队列深度）+ 运行队列表（getJobs）+ 记忆会话（getSessions）+ **RBAC 矩阵**（由 /api/roles 的 permissions 动态生成，动作并集 × 角色列，✓/— 标记）。
  - `styles.ts` 增 `.hero/.cards/.kpi/.pill.running|queued|done|ready|error|cancelled/.matrix/.role-badge/.kv/.link/.two` 等语义类；`main.ts` 注册两个新组件。
  - 校验：`tsc --noEmit` + `vite build` 全绿（31 模块，57.6KB）。RBAC 矩阵为真实数据驱动，服务端新增动作/角色自动同步。
