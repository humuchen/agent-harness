# 2026-08-17 工作日志

## 智能客服插件化 · 实施清单延续（请求三之后续）
- 用户「continue」指令下，把 `docs/agent-plugin-architecture.md`（请求三架构方案）落成可执行任务清单。
- 新增 `docs/agent-plugin-implementation-plan.md`：Phase 0（core 新增 `plugin/context.ts`+`plugin/module.ts`，改 `loader.ts` 加 `registerLocalModule`+`enable` 调 `mod.setup(ctx)`）→ Phase 1（server 新增 `plugin-bootstrap.ts`/`plugin-ext.ts`，改 `server.ts`）∥ Phase 2（webapp 新增 `plugin-ui-registry.ts`，改 `app.ts`）→ Phase 3（`plugins/customer-service/` 插件包）→ Phase 4（生产加固：共享 Memory 后端/热插拔）。
- 新增 `docs/agent-plugin-phases.svg`：阶段依赖关键路径图。
- 关键约束重申：core/server/webapp 三层零业务耦合（无客服业务词），业务语义只存在于 `plugins/customer-service/`；唯一生产硬缺口=多副本 ChatSession/Memory 共享后端（RWX 卷+file 后端）。
- 仍停留在「接口规范+文件改动点」层面，未写业务实现代码（遵循用户「仅提供方案」要求）。
- 待用户确认下一步：编码 Phase 0 骨架 / 补插件依赖 ER 图 / 细化 manifest 接驳检查表。

## 智能客服插件化 · 编码执行（Phase 0→3 全落地）
- 用户「按照实施任务清单执行」→ 实际编码落地（从「仅方案」转为实现）。网络多次中断，已逐文件确认落盘状态后继续。
- Phase 0（core 业务契约，已落）：`plugin/context.ts`(PluginContext 注入面+`getPluginToolRegistry` 共享工具表)、`plugin/module.ts`(PluginModule 生命周期)、`loader.ts` 加 `installModule`/`enable` 调 `setup(ctx)`→`mergePluginTools`→`onStart`、并 `toAgentCard` 透传 `assembly`；`manifest.ts` 加可选 `assembly?: AgentAssembly`；`plugin/index.ts` 导出。
- Phase 1（server 扩展点）：`plugin-ext.ts` 加 `WebPluginHost`（collect `PluginUIView`→`listViews()`）；`plugin-bootstrap.ts` 新：`createPluginSystem()`（注入双宿主+复用共享 `getAgentRegistry()`）+`bootstrapPlugins()`（动态 require 默认/AGENT_PLUGINS 指定入口，单插件失败不阻断）；`server.ts` 在 bootstrap（initAgentRegistry 之后）构造 `pluginSystem`、启用插件，加 `/api/plugins` 元数据端点与插件路由前缀 `/api/plugins/:id/*`；`runner.ts` `assembleAgent` 合并 `getPluginToolRegistry()` 进运行工具集。
- Phase 2（webapp 扩展点）：`plugin-ui-registry.ts` 新（`PluginUIRegistry` 单例）；`app.ts` 拉取 `/api/plugins` 动态渲染插件 Tab（`unsafeHTML` 注入 `render()` 片段）。
- Phase 3（customer-service 插件包）：`plugins/customer-service/` 独立包（package.json workspace `plugins/*` 已加；tsconfig 用 paths 指 core dist）。含 `manifest.ts`(assembly.systemPrompt=buildSystemPrompt)、`prompts.ts`、`store.ts`(进程内会话/满意度)、`tools/faq.ts`(faq_search)、`tools/ticket.ts`(ticket_create/satisfaction_record)、`workflows/conversation.ts`(DAG 示例)、`server/cs-routes.ts`(转人工/满意度/意图 HTTP 路由)、`web/admin-panel.ts`(管理后台 Tab)、`src/index.ts`(PluginModule 主入口)。
- 关键修正（避免回归）：`createPluginSystem()` 必须放在 `initAgentRegistry(store)` 之后调用，否则模块加载期 `getAgentRegistry()` 会提前创建 volatile 单例使持久后端失效；loader 复用共享 registry 才能被路由发现。
- ⚠️ 沙箱限制：pnpm install 被安全守卫拦截，无法编译校验（无 tsc/node_modules）。代码按 core 公共 API 严格手写类型；需用户在可装依赖环境跑 `pnpm -r build` 验证。Phase 4（生产共享 Memory 后端/热插拔 UI）未做。
- 交付文件：core `plugin/{context,module}.ts` + `loader.ts`+`manifest.ts`；server `plugin-bootstrap.ts`+`plugin-ext.ts`+`server.ts`+`runner.ts`；webapp `plugin-ui-registry.ts`+`app.ts`；`plugins/customer-service/*`（12 文件）；`pnpm-workspace.yaml`。
