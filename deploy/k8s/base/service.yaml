apiVersion: v1
kind: Service
metadata:
  name: agent-harness
  namespace: agent-harness
  labels:
    app.kubernetes.io/part-of: agent-harness
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-harness
  ports:
    - name: http
      port: 4173
      targetPort: http
      protocol: TCP
