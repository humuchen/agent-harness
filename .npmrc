# pnpm v11 在 CI 里会在 `pnpm run` 前做 verifyDepsBeforeRun 依赖校验，
# 判定 node_modules 与 lockfile 不同步时会派生一个嵌套的 `pnpm install`（CI 下默认 --frozen-lockfile），
# 该嵌套 install 一旦失败（lockfile 漂移/网络/构建脚本）就会 abort 整个构建（render 报 "pnpm install exit 1"）。
# 在 CI 构建链路里显式关闭它：构建前已由 `pnpm install --no-frozen-lockfile` 同步过依赖，无需再自检。
verify-deps-before-run=false

# 沿用 pnpm-workspace.yaml 的构建脚本放行策略（strictDepBuilds 默认 true，仅 esbuild 等显式放行）。
# 此处不再重复声明，避免与 workspace 配置冲突。
