{
  "name": "@agent-harness/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Web Playground (zero-dependency SSE dashboard) for @agent-harness/core.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json && node dist/server.js",
    "test": "node --test test/*.test.cjs"
  },
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@agent-harness/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "optionalDependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.4.5"
  }
}
